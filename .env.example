# Copy to .env and fill in values before running docker compose
# .env is gitignored — never commit credentials

POSTGRES_DB=plm
POSTGRES_USER=plm
POSTGRES_PASSWORD=changeme

# Optional: change the host port if 5432 is already in use
POSTGRES_PORT=5432
